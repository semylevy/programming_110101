#include "arrays.h"
#include "russe.h"
#include "multi.h"
#include "contar5.h"
#include "arraypointers.h"
#include <stdio.h>

int main()
{
    /*int size;
    printf("Tamaño del arreglo\n");
    scanf("%i", &size);
    
    int a[size];
    
    for (int i=0; i<size; ++i)
    {
        printf("Valor %i\n", i);
        scanf("%i", &a[i]);
    
    }
    
    imprimeNprim(a, 2, size);
    printf("\n");
    imprimeNultim(a, 2, size);
    printf("\n");
    printf("El número mayor del arreglo es %i\n", imprimeMayor(a, size));
    printf("El número menor del arreglo es %i\n", imprimeMenor(a, size));
    
    return 0;

    printf("%i\n", russe(316, 52));
    
    printf("%i", multiplicaPart(55,2));
    
    int var=contar5();*/
    
    
}

